# 技术栈

- 前端：Svelte 5、SvelteKit、TypeScript、Vite、Tailwind CSS。
- 后端：Python 3.11、FastAPI、Uvicorn、SQLAlchemy、Alembic。
- 默认数据：SQLite、本地文件、Chroma。
- Python 环境：`.venv`；依赖锁为 `uv.lock`，运行时不依赖 uv。
- 本机 PyTorch：`2.8.0+cpu`，来自 `E:\dependency\torch-2.8.0-cp311-cp311-win_amd64.whl`。
- 兼容目标：Chrome 108；回归环境为 `E:\Playwright\1.28.1` 的 Chromium `108.0.5359.29`。
